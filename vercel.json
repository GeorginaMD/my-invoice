{
  "buildCommand": "cd my-invoice-front/my-invoice-front && npm install && npm run build",
  "outputDirectory": "my-invoice-front/my-invoice-front/dist",
  "rewrites": [
    { "source": "/analyze/:path*", "destination": "/api/server" },
    { "source": "/server-check", "destination": "/api/server" }
  ]
}